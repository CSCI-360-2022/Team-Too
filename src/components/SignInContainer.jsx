import React from 'react'
import { withAuthenticator } from '@aws-amplify/ui-react'

function SignInContainer() {
  return (
    <div>SignInContainer</div>
  )
}

export default withAuthenticator(SignInContainer)